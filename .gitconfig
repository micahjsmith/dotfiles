[user]
    name = Micah Smith
    email = micahjsmith@gmail.com
[github]
	user = micahjsmith
[alias]
    br  = branch
    ch  = checkout
    co  = commit
    dc  = difftool
    d   = difftool -g
    gr  = grep
    lg  = log --graph --all --full-history --color --abbrev-commit --decorate
    lgp = log --graph --all --full-history --color --pretty=format:'%C(yellow)%h%Creset%x09%Cblue%d%Creset %s <%an>'
    mc  = mergetool
    m   = mergetool --tool=meld
    st  = status
[diff]
    tool = vimdiff
    guitool = meld
[difftool]
    prompt = false
[difftool "vimdiff"]
    # vim will otherwise fail on filenames starting with a '+'
    cmd = vimdiff -- $LOCAL $REMOTE
[branch]
    autosetuprebase = always
[merge]
    tool = vimdiff
    conflictstyle = diff3
[mergetool]
    prompt = false
[push]
    default = tracking
[core]
    # warn on whitespace issues
    whitespace = trailing-space,space-before-tab
[apply]
    # fix whitespace issues in patches
    whitespace = fix
[include]
    path = ~/.gitconfig.local
[url "git@github.com:"]
	insteadOf = https://github.com/
